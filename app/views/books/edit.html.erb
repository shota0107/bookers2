<h1>Books#edit</h1>
<%= form_with model: Book.new do |f| %>
  <h4>Name</h4>
  <%= f.text_field :title %>

  <h4>Introduction</h4>
  <%= f.text_area :body %>

  <%= f.submit 'Creat Book' %>
  <% end %>